# Helper: interp cancel with a custom result message.
# Per TIP #285: first arg after switches is the interp path
# ("" = current), second is the result message.
interp cancel -- "" "custom cancel message"
